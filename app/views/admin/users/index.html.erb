<h1>All Users</h1>

<% @users.each do |user|%>
	<p>
		<%=user.username%>
		<%=user.available_points%>
		
		<p>Set New Point Total:</p>
		<%=render partial: 'edit', locals: {user: user}%> 
	</p><br />

	<%end%>
