<h1>Available Rewards:</h1>
<% @rewards.each do |reward| %>
  <p><%=reward.name%></p>
  <p><%=reward.description%></p>
  <p><%=reward.price%></p>
	<%= link_to "Purchase", '/rewards/purchase', method: "patch"%>
  <% end %>
